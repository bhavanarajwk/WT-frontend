---
description: Prevent Cursor from being added as a git co-author or commit contributor
alwaysApply: true
---

Never add `Co-authored-by: Cursor`, `Made-with: Cursor`, or any Cursor attribution trailer to git commits or pull requests.

When committing, use plain `git commit` messages only — no `--trailer` flags and no co-author lines.
